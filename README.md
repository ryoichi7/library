# library
# 2 entities: Person, Book; OnetoMany relation
# core functionality :
#  CRUD with person, CRUD with book,  book attachment/detachment to person; Search books in db by (partial) name; Book rent expiry functionality (presents in Person page)
